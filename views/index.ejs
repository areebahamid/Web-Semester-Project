<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(error.length> 0){ %>
        <div id="error-alert" class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-red-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= error %>
            </span>
        </div>
        <%}%>
            <div class="bg-zinc-900 w-full h-screen text-white">
                <h1 class="font-bold text-3xl pt-10 px-6 mb-5">Welcome to Registeration</h1>
                <form action="/users/register" method="post">
                    <input type="text" placeholder="Ful Name" name="fullName"
                        class="bg-transparent bg-zinc-700 ml-5 mr-1 rounded-lg w-34 h-10 p-2 outline-none" />
                    <input type="text" placeholder="Email" name="email"
                        class="bg-transparent bg-zinc-700 m-1 rounded-lg w-34 h-10 p-2 outline-none" />
                    <input type="password" placeholder="Password" name="password"
                        class="bg-transparent bg-zinc-700 m-1 rounded-lg w-34 h-10 p-2 outline-none" />
                    <input type="number" placeholder="Contact No" name="contact"
                        class="bg-transparent bg-zinc-700 m-1 rounded-lg w-34 h-10 p-2 outline-none" />
                    <input type="submit" placeholder="Signup"
                        class=" bg-blue-600 m-1 rounded-lg w-34 h-10 p-2 outline-none cursor-pointer" />
                </form>
                <h1 class=" pt-10 px-6">already have account ? :->
                    <a class="ml-6 p-3 rounded-xl bg-zinc-700" href="/login">Login</a>
                </h1>

            </div>
</body>
<script>
    // Set timeout to hide the alerts after 2 seconds
    setTimeout(() => {
        const errorAlert = document.getElementById('error-alert');

        if (errorAlert) {
            errorAlert.classList.add('opacity-0'); // fade out
            setTimeout(() => errorAlert.style.display = 'none', 500); // hide after transition
        }
    }, 2000); // Display duration (2 seconds)
</script>

</html>